{
    "version":2,
    "buildCommand": "next build",
    "Builds":[
        {"src":"*.js","use":"@vercel/node"}
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"/"
        }
    ]
}